<!DOCTYPE html>
%html
  %head
    %title PDF Merger
    = stylesheet_link_tag 'application', media:'all', 'data-turbolinks-track'=>true
    = javascript_include_tag 'application', 'data-turbolinks-track'=>true
    = csrf_meta_tags
  %body.container
    .header
      %h3.text-muted PDF Merge
      = if flash[:notice]
        / TODO fix
        .alert.alert-danger{role:"alert"}
          = flash[:notice]
    = yield
    .footer
      %p
        / TODO: fix space between copyright and company name
        ©42Gems
        = Time.now.strftime("%Y")
